// Web stub — react-native-agora is native-only.
// .native.tsx files are still bundled on web via require.context;
// this stub satisfies their imports without pulling in native code.

export const ClientRoleType = {
  ClientRoleBroadcaster: 1,
  ClientRoleAudience: 2,
};
